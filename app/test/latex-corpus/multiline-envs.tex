% Synthetic corpus: amsmath environments carrying across lines.
\begin{equation}
  V(a, z) = \max_{c, a'} \left\{ u(c) + \beta \mathbb{E}\, V(a', z') \right\}
\end{equation}
Text between environments.
\begin{align}
  y_t &= \alpha + \beta x_t + \varepsilon_t \\
  z_t &= \gamma + \delta w_t + \eta_t
\end{align}
\begin{align*}
  f(x) &= (x+1)^2 \\
       &= x^2 + 2x + 1
\end{align*}
\begin{cases}
  1 & \text{if } x > 0 \\
  0 & \text{otherwise}
\end{cases}
\begin{pmatrix}
  a & b \\
  c & d
\end{pmatrix}
\begin{gather}
  a = b \\
  c = d
\end{gather}
\begin{multline}
  a + b + c + d \\
  + e + f
\end{multline}
Final plain line.
