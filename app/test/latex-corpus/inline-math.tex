% Synthetic corpus: inline math ($…$ and \(…\)). All text is fictional.
\section{Inline mathematics}
The Euler identity $e^{i\pi} + 1 = 0$ sits inside running text.
A rate $\beta \in (0,1)$ and a shock $\varepsilon_t \sim N(0, \sigma^2)$ appear here.
Paren form: the map \(f\colon X \to Y\) is continuous, and \(g(x) = x^2 + 3x - 7\).
Two zones on one line: $a+b$ then plain words then $c \cdot d$ again.
Escaped dollar \$100 must stay text, but $p = \$3$ is odd yet legal-ish.
An unclosed inline dollar here $x + y and the zone must die at end of line.
The next line is plain text and must not be washed.
Back to normal: $\alpha_1^{k}$ with subscripts_and^carets outside math.
Adjacent: $a$$b$ and empty $$ pair reading as display opener is tested elsewhere.
Ties: $x_1, x_2, \ldots, x_n$ and operators $x \leq y \neq z$.
