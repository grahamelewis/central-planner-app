% Synthetic corpus: verbatim-family environments and \verb.
\begin{verbatim}
raw $dollars$ and \commands and % percent stay literal here
  even {braces} and $$display$$ markers
\end{verbatim}
After verbatim, math works again: $x^2$.
\begin{lstlisting}
for i in 1:10   # julia-ish, $ not math
    push!(xs, i)
end
\end{lstlisting}
\begin{minted}{python}
print("hello $USER")
\end{minted}
\begin{Verbatim}
Capital-V Verbatim body with $ and \foo
\end{Verbatim}
Inline: \verb|x = $y + z$| then normal text with $real$ math.
Star form: \verb*|spaced $ arg| and then more text.
Other delims: \verb+plus $ delimited+ and \verb!bang $ delimited!.
\begin{alltt}
alltt body: $ stays literal
\end{alltt}
\begin{filecontents}{notes.txt}
file contents body $ literal
\end{filecontents}
\begin{comment}
commented-out block: $x$ \alpha
\end{comment}
End-of-line \end{verbatim} appearing in text mode above is fine; done.
