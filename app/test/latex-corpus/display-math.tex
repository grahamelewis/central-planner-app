% Synthetic corpus: display math — $$…$$, \[…\], carrying across lines.
Before the display.
$$
\int_0^\infty e^{-x^2}\,dx = \frac{\sqrt{\pi}}{2}
$$
Between displays, plain text with a word like sum that is not math.
\[
\sum_{k=1}^{n} k = \frac{n(n+1)}{2}
\]
One-line bracket display: \[ a^2 + b^2 = c^2 \] then trailing text.
One-line double-dollar: $$ E = mc^2 $$ then trailing text.
A display that opens and carries:
\[
  x_{t+1} = A x_t + B u_t
  + w_t
\]
Done.
