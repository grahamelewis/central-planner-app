{
  "name": "central-planner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "server.js",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.mjs",
    "test:fast": "node --test $(ls test/*.test.mjs | grep -v '/ui\\.')",
    "test:ui": "node --test test/ui.*.test.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.260",
    "express": "^4.19.0",
    "monaco-editor": "0.56.0",
    "pdfjs-dist": "^6.1.200",
    "tsx": "4.23.13",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "playwright-core": "^1.60.0",
    "typescript": "7.0.2"
  }
}
